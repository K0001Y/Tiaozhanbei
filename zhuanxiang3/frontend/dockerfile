# 使用官方 Node.js 镜像作为基础镜像
FROM node:16

# 设置工作目录
WORKDIR /app

# 复制 package.json 和 package-lock.json
COPY frontend/package*.json ./

# 安装依赖
RUN npm install

# 复制其余的项目文件
COPY frontend/ .

# 构建前端应用
RUN npm run build

# 暴露应用端口
EXPOSE 3000

# 启动前端应用
CMD ["npm", "run", "dev"]
