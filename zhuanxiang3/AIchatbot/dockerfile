# 使用官方 Python 镜像作为基础镜像
FROM python:3.11-slim

# 设置工作目录
WORKDIR /app

# 复制 requirements.txt 并安装依赖
COPY AIchatbot/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# 复制其余的项目文件
COPY AIchatbot/ .

# 暴露应用端口
EXPOSE 8080

# 启动 AI 服务
CMD ["python", "server.py"]
